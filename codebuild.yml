version: 0.2
phases:
 
  build:
    commands:
      - pwd
      - cd /codebuild/output/src107605912/src/amplifyapp/build
      - zip -r ../deployment.zip
      - python3 amplify-build.py --app-name "app-name2" --branch-name "main" --dep-loc ./deployment.zip
  
